# Allow firmware_file access to load Non-HLOS images
r_dir_file(ueventd, firmware_file)

allow ueventd firmware_file:blk_file { create setattr };
allow ueventd bt_firmware_file:blk_file { create setattr };
